import * as Library from "../library/index";
import {Simple} from './simple';
/**
 * Class: Category
 */
export class Category extends Simple {
  /**
   * Constructor()
   * @param options
   */
  constructor (options) {
    super(options);
  }
}
