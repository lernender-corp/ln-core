import * as Library from "../library/index";
import {Simple} from "./simple";

/**
 * Class: Role
 */
export class Role extends Simple {
  /**
   * Constructor()
   * @param options
   */
  constructor(options) {
    super(options);
  }
}
